<resources>

    <!-- Base application theme. -->
    <style name="Theme.AndroidAcademyAssignments" parent="Theme.MaterialComponents.DayNight.NoActionBar">
        <item name="android:statusBarColor">@color/background</item>
        <item name="windowActionBar">false</item>
        <item name="windowNoTitle">true</item>
    </style>

    <style name="Theme.AndroidAcademyAssignments.AppBarOverlay" parent="ThemeOverlay.AppCompat.Dark.ActionBar" />

    <style name="Theme.AndroidAcademyAssignments.PopupOverlay" parent="ThemeOverlay.AppCompat.Light" />

</resources>